import { Box } from "@chakra-ui/core"
import * as React from "react"

const IconContainer = (props: any) => (
  <Box {...props} bg="gray.50" boxShadow="0 0 1px 3px rgba(0, 0, 0, .125)" />
)

export default IconContainer
