import * as React from "react"
import Headroom from "react-headroom"
import "./Header.css"

const HeaderContainer = (props: any) => (
  <Headroom disableInlineStyles {...props} />
)

export default HeaderContainer
